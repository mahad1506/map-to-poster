# `src/map/geocoder.js`

Summary
- Provides `searchLocation()` for geocoding and a `formatCoords()` helper for display.
- Integrated into UI to resolve user search input and update `state.lat`/`state.lon`.

Notes
- Verify the geocoding provider rate limits and API keys (if any).

Source: ../src/map/geocoder.js
