# `main.js` and `index.html`

`main.js`
- Bootstraps the app and wires `setupControls()` and `initMap()`.
- Entrypoint for application logic.

`index.html`
- Application shell and DOM containers: `map`, `artistic-map`, controls.

Notes
- Confirm CSS load order for proper map container sizing.
- Vite dev server used in development (`npm run dev`).
