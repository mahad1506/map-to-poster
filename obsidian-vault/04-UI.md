# UI notes (`src/ui`)

`form.js`
- Binds DOM controls to `state` and map functions.
- Implements search, presets, theme selection, marker and route toggles, custom theme editor flows.
- Builds UI fragments for the artistic theme picker and handles modal interactions.

Other UI files
- `src/ui/form.js` — main control bindings

Useful link
- [src/ui/form.js](src/ui/form.js)
